import { defineConfig } from 'vitepress'

// https://vitepress.vuejs.org/config/app-configs
export default defineConfig({
  title: 'MCPForm',
  description: 'Schema-driven React forms with deterministic MCP-compatible tool definitions.',
  themeConfig: {
    siteTitle: 'MCPForm'
  }
})
